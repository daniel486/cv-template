/*
 * CertificationTitleField Messages
 *
 * This contains all the text for the CertificationTitleField component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CertificationTitleField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CertificationTitleField component!',
  },
});
