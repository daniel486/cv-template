/*
 * EducationField Messages
 *
 * This contains all the text for the EducationField component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EducationField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EducationField component!',
  },
});
