/*
 * SkillField Messages
 *
 * This contains all the text for the SkillField component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SkillField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SkillField component!',
  },
});
