/*
 * WorkExperienceField Messages
 *
 * This contains all the text for the WorkExperienceField component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WorkExperienceField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkExperienceField component!',
  },
});
