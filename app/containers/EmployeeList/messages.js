/*
 * EmployeeList Messages
 *
 * This contains all the text for the EmployeeList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EmployeeList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmployeeList container!',
  },
});
