// import { selectEmployeeListDomain } from '../selectors';

describe('selectEmployeeListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
