// import { selectCertificationFieldDomain } from '../selectors';

describe('selectCertificationFieldDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
