/*
 * CertificationField Messages
 *
 * This contains all the text for the CertificationField container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CertificationField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CertificationField container!',
  },
});
